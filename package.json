{
  "name": "melange-opam-template",
  "scripts": {
    "preopam-install": "opam update",
    "opam-install": "opam install -y . --deps-only --with-test",
    "check-npm-deps": "opam exec opam-check-npm-deps",
    "preinit": "opam switch create . 5.1.0 -y --deps-only",
    "init": "npm install && npm run opam-install && npm run check-npm-deps",
    "build": "dune build @react @node",
    "build-verbose": "dune build --verbose @react @node",
    "clean": "dune clean",
    "format": "dune build @fmt --auto-promote",
    "format-check": "dune build @fmt",
    "watch": "dune build --watch @react @node",
    "bundle": "webpack --mode production --entry ./_build/default/src/output/src/ReactApp.js",
    "serve": "webpack serve --open --mode development --entry ./_build/default/src/output/src/ReactApp.js"
  },
  "scriptsComments": {
    "preopam-install": "# Sync opam database with upstream repositories: https://opam.ocaml.org/doc/Usage.html#opam-update",
    "opam-install": "# Downloads, builds and installs opam pkgs: https://opam.ocaml.org/doc/Usage.html#opam-install",
    "check-npm-deps": "# Checks that Melange bindings have their JS dependencies available: https://github.com/ahrefs/opam-check-npm-deps",
    "preinit": "# Create opam switch: https://opam.ocaml.org/doc/Usage.html#opam-switch",
    "init": "# Prepare everything to work in development mode (run just once, for initialization)",
    "build": "# Build the Melange apps",
    "build-verbose": "# Build the Melange apps in verbose mode",
    "clean": "# Cleans all Melange artifacts",
    "format": "# Formats the Melange sources using ocamlformat",
    "format-check": "# Checks that the Melange sources have the right formatting (read-only)",
    "watch": "dune build --watch @react @node",
    "bundle": "# Bundle the JavaScriptapps generated by Melange",
    "serve": "# Serves the React app in a local server"
  },
  "dependencies": {
    "react": "^17.0.0",
    "react-dom": "^17.0.0"
  },
  "devDependencies": {
    "webpack": "^5.73.0",
    "webpack-cli": "^4.9.2",
    "webpack-dev-server": "^4.9.1"
  }
}
